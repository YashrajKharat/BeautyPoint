# ============================================
# BEAUTY POINT ECOMMERCE - ENVIRONMENT SETUP
# ============================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# ============================================
# SERVER CONFIGURATION
# ============================================
PORT=5000
NODE_ENV=production

# ============================================
# DATABASE
# ============================================
# MongoDB Atlas Connection String
# Format: mongodb+srv://username:password@cluster.mongodb.net/database
MONGODB_URI=mongodb+srv://ecommerce-new:EcommerceProject@cluster0.accnhcz.mongodb.net/ecommerce?retryWrites=true&w=majority&appName=Cluster0

# ============================================
# JWT & AUTHENTICATION
# ============================================
# Generate a strong secret key (minimum 32 characters)
# Command: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secure-32-character-or-longer-random-string-here

# ============================================
# FRONTEND CONFIGURATION
# ============================================
# Frontend URL for CORS
FRONTEND_URL=https://yourdomain.com
# Development: http://localhost:5173
# Production: https://yourdomain.com

# ============================================
# EMAIL CONFIGURATION
# ============================================
# Gmail SMTP Settings
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password-from-gmail

# Optional: Custom SMTP Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# ============================================
# API RATE LIMITING
# ============================================
# Requests per time window
API_RATE_LIMIT=100

# ============================================
# SESSION & TOKEN CONFIGURATION
# ============================================
# Token expiry time (milliseconds)
# 7 days = 604800000
SESSION_TIMEOUT=604800000

# ============================================
# LOGGING & MONITORING
# ============================================
# Optional: Sentry DSN for error tracking
SENTRY_DSN=https://your-sentry-dsn@sentry.io/1234567

# Optional: New Relic License Key
NEW_RELIC_LICENSE_KEY=your-new-relic-key

# ============================================
# SECURITY SETTINGS
# ============================================
# Allow requests only from specific IP (optional)
# ALLOWED_IPS=192.168.1.1,10.0.0.0/8

# Enable CORS preflight
CORS_MAX_AGE=86400

# ============================================
# NOTES FOR PRODUCTION
# ============================================
# 1. Generate a strong JWT_SECRET:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 2. Use MongoDB Atlas:
#    - Create cluster on https://www.mongodb.com/cloud/atlas
#    - Get connection string
#    - Add IP whitelist
#    - Enable backups
#
# 3. Gmail Configuration:
#    - Enable 2FA on Google Account
#    - Create App Password: https://myaccount.google.com/apppasswords
#    - Use App Password in EMAIL_PASSWORD
#
# 4. HTTPS/SSL:
#    - Use Let's Encrypt for free SSL
#    - Redirect HTTP to HTTPS
#    - Enable HSTS header
#
# 5. Keep .env secure:
#    - Never commit to version control
#    - Use environment variable management in hosting platform
#    - Rotate credentials regularly
#    - Enable secret rotation in production
